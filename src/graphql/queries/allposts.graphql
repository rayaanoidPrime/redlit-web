query Allposts($limit: Int, $cursor: String) {
  allposts(limit: $limit, cursor: $cursor) {
    posts {
      id
      createdAt
      updatedAt
      title
      text
      points
      authorId
    }
    hasMore
  }
}